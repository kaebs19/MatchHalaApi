// MatchHala - Mobile API v3
// يحتوي فقط على الـ endpoints الجديدة أو المعدلة في v3
// أي endpoint غير موجود هنا → يُستخدم من v2 (أو v1) تلقائياً

const express = require('express');
const router = express.Router();
const { protect } = require('../../middleware/auth');

// ============================================
// v3 Endpoints — الجديدة أو المعدلة فقط
// ============================================

// --- أضف endpoints v3 هنا ---


module.exports = router;
